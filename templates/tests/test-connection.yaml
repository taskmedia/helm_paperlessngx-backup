apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "plngxbackup.fullname" . }}-test-connection"
  labels:
    {{- include "plngxbackup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "plngxbackup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
